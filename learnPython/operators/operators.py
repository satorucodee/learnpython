# python operators
### Operator can be used to perform operation on values and variables

print(10+10); # 20

# Arithmetic operators //HACK: +, -, *, /, %,, **, //
# Assignment operators //HACK: =, +=, -=, *=, /=, %=, //=, **=, &=, |=, ^=, >>=, <<= :=
# Comparison operators //HACK: ==, !=, >, <, >=, <=
# Logical operators   //HACK: and, or , not
# Identity operators //HACK: is, is not
# Membership operators //HACK: in, not in
# Bitwise operators  //HACK: &, |, ^, ~, <<, >>
